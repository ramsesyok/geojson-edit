{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (技術検証)",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/out/extension/**/*.js"],
      "preLaunchTask": "npm: build"
    }
  ]
}
